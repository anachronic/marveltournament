<p id="notice"><%= notice %></p>

<h1>Peleadores <div class="float-right"><%= link_to 'Agregar Peleador', new_persona_path, :class=>"btn btn-primary" %></div></h1>


<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Nombre</th>
      <th scope="col">Email</th>
      <th scope="col">Está vivo</th>
    </tr>
  </thead>
  <tbody>
    <% @personas.each_with_index do |persona, index| %>
      <tr>
        <th scope="row"><%= index + 1 %></th>
        <td><%= link_to persona.name, persona %></td>
        <td><%= persona.email %></td>
        <td><%= persona.alive %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Alive</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @personas.each do |persona| %>
      <tr>
        <td><%= persona.name %></td>
        <td><%= persona.email %></td>
        <td><%= persona.alive %></td>
        <td><%= link_to 'Show', persona %></td>
        <td><%= link_to 'Edit', edit_persona_path(persona) %></td>
        <td><%= link_to 'Destroy', persona, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
